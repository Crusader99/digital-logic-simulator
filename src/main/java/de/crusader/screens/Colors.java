package de.crusader.screens;

import java.awt.Color;

import lombok.Getter;

@Getter
public class Colors {
	/*
	 * The basic colors for the graphic components are defined here.
	 */

	private final Color background = new Color(45, 45, 48);

	private final Color foreground = new Color(76, 74, 72);

	private final Color focus = new Color(0, 173, 238);

	private final Color darkFocus = new Color(4, 100, 200);

}