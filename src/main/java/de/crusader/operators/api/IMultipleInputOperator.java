package de.crusader.operators.api;

public interface IMultipleInputOperator {
	/*
	 * If an operator is instance of this interface the operator will accept
	 * multiple input variables.
	 */
}
